package constants

const CYCLE_SHIFT = 1
const CYCLE_DIVISOR = 1 << CYCLE_SHIFT

const (
	CYCLE_2  = uint64(2 / CYCLE_DIVISOR)
	CYCLE_4  = uint64(4 / CYCLE_DIVISOR)
	CYCLE_6  = uint64(6 / CYCLE_DIVISOR)
	CYCLE_8  = uint64(8 / CYCLE_DIVISOR)
	CYCLE_12 = uint64(12 / CYCLE_DIVISOR)
	CYCLE_18 = uint64(18 / CYCLE_DIVISOR)
	CYCLE_24 = uint64(24 / CYCLE_DIVISOR)
	CYCLE_40 = uint64(40 / CYCLE_DIVISOR)
)

const (
	PAL_BASE_FREQUENCY  = 21_281_370 / CYCLE_DIVISOR
	NTSC_BASE_FREQUENCY = 1_890_000_000 / 88 / CYCLE_DIVISOR
	SPU_BASE_FREQUENCY  = 1_024_000
)
